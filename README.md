# MARDUCK RIS Agendamento

Sistema simples de agendamento de exames médicos (RIS) com backend Node.js + SQLite e frontend HTML.

## Como usar

### 1. Backend

```bash
cd backend
npm install
npm start
